---
const { file } = Astro.props;
---

<code>{file}</code>

<style>
@layer components {
    code {
        color: GrayText;
        font-family: inherit;
        font-size: .9rem;

        @media (min-width: 992px) {
            /* grid-column: edge; */
        }
    }
}
</style>